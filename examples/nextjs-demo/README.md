# Next.js demo for Lookout

```bash
pnpm install
pnpm dev
```

Optional seeded bug (broken create-key button):

```bash
set SEED_BUG=1&& pnpm dev
```

On Windows PowerShell:

```bash
$env:SEED_BUG=1; pnpm dev
```
